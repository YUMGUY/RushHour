using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class seatProperties : MonoBehaviour
{
    // Start is called before the first frame update

    public bool seatOpen;

    private void Awake()
    {
        seatOpen = true;
    }
    void Start()
    {
       
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
